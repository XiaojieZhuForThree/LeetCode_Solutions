class Solution {
    public String fractionToDecimal(int numerator, int denominator) {
        StringBuilder ans = new StringBuilder();
        int div = numberator / denominator;
        int remainder = numberator % denominator;
        ans.append(div);
        if (remainder == 0){
            return ans.toString();
        }
        Set<Integer> seen = new HashSet<>();
    }
}
