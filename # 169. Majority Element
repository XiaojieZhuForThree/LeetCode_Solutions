class Solution {
    public int majorityElement(int[] nums) {
        int count = 0;
        int res = nums[0];
        for (int i : nums) {
            if (count == 0) {
                res = i;
            }
            count += (i == res) ? 1 : -1;
        }
        return res;
    }
}
