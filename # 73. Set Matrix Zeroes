class Solution {
    public void setZeroes(int[][] matrix) {
        Set<Integer> delRow = new HashSet<>();
        Set<Integer> delCol = new HashSet<>();
        for (int i = 0; i < matrix.length; i++) {
        	for (int j = 0; j < matrix[0].length; j++) {
        		if (matrix[i][j] == 0) {
        			delRow.add(i);
        			delCol.add(j);
        		}
        	}
        }
        for (int i = 0; i < matrix.length; i++) {
        	for (int j = 0; j < matrix[0].length; j++) {
        		if (delRow.contains(i) || delCol.contains(j)) {
        			matrix[i][j] = 0;
        		}
        	}
        } 
    }
}
